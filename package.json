{
  "name": "microservice-testing",
  "version": "1.0.0",
  "description": "1. To work with project as intended use `pip install poetry`\r 2. Install dependencies `cd` into the directory where the `pyproject.toml` is located then `poetry install`\r 3. Run the DB migrations via poetry `poetry run python app/prestart.py` (only required once)\r 4. Run the FastAPI server via poetry with the Python command: `poetry run python app/main.py`\r 5. Open `http://localhost:8001/` docs to see service documentation",
  "main": "commitlint.config.js",
  "directories": {
    "doc": "doc",
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://git-place.gs-labs.tv/itmo/contract-testing.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@commitlint/config-angular": "^17.0.0"
  }
}
